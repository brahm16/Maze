{% extends 'AgentFinancier.html.twig' %}

{% block body %}
    <div class="card">
        <div class="card-body">
            <h1>Facture creation</h1>

            {{ form_start(form) }}
            {{ form_widget(form) }}
            <input type="submit" value="Create" class="btn btn-primary" />
            {{ form_end(form) }}
            <br>
            <a href="{{ path('facture_index') }}" class="btn btn-warning">Back to the list</a>
        </div>
    </div>

{% endblock %}
