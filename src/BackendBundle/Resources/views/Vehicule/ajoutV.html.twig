{% extends 'AgentTransport.html.twig' %}
{% block body %}
    <div class="card">
        <div class="card-body">
            {{ form_start(form) }}
            {{ form_widget(form) }}
            <input type="submit" value="Ajouter Vehicule" class="btn btn-success">

            {{ form_end(form) }}        </div>
    </div>
{% endblock %}

